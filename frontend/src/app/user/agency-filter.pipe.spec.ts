import { AgencyFilterPipe } from './agency-filter.pipe';

describe('AgencyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgencyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
